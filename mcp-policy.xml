<!--
  Policy for an APIM "Expose an existing MCP server" API.

  Configure https://api.githubcopilot.com/mcp/ as the backend URL and select
  Streamable HTTP when creating the MCP server in APIM. Backend routing belongs
  to the native MCP configuration, not this policy.

  Before applying this policy, create the following in APIM Credential manager:
    Provider:   github-mcp (GitHub, OAuth 2.0 authorization code grant)
    Connection: github-mcp-connection

  The GitHub OAuth app callback URL must be:
    https://authorization-manager.consent.azure-apim.net/redirect/apim/<APIM-NAME>

  This policy never reads context.Response.Body. MCP responses are streamed, and
  buffering the response body breaks Streamable HTTP.
-->
<policies>
  <inbound>
    <!--
      The APIM portal's MCP test client runs in a browser. Keep this policy
      first so APIM can answer OPTIONS preflight requests before OAuth runs.
    -->
    <cors allow-credentials="true" terminate-unmatched-request="true">
      <allowed-origins>
        <origin>https://portal.azure.com</origin>
        <origin>https://apimanagement.hosting.portal.azure.net</origin>
      </allowed-origins>
      <allowed-methods preflight-result-max-age="300">
        <method>GET</method>
        <method>POST</method>
        <method>DELETE</method>
        <method>OPTIONS</method>
      </allowed-methods>
      <allowed-headers>
        <header>Accept</header>
        <header>Authorization</header>
        <header>Content-Type</header>
        <header>Mcp-Protocol-Version</header>
        <header>Mcp-Session-Id</header>
        <header>Ocp-Apim-Subscription-Key</header>
        <header>x-correlation-id</header>
      </allowed-headers>
      <expose-headers>
        <header>Mcp-Session-Id</header>
        <header>WWW-Authenticate</header>
        <header>x-correlation-id</header>
      </expose-headers>
    </cors>

    <!-- Capture these values before <base /> so on-error can always use them. -->
    <set-variable name="correlationId" value='@(context.Request.Headers.GetValueOrDefault("x-correlation-id", context.RequestId.ToString()))' />
    <set-variable name="mcpRequestId" value='@{
      try
      {
        var request = context.Request.Body.As&lt;JObject&gt;(preserveContent: true);
        var id = request?["id"];
        return id == null ? "null" : id.ToString(Newtonsoft.Json.Formatting.None);
      }
      catch
      {
        return "null";
      }
    }' />

    <base />

    <!--
      Obtain the GitHub user token from APIM Credential manager. This replaces
      the Entra on-behalf-of flow, which cannot mint a GitHub access token.
    -->
    <get-authorization-context
      provider-id="github-mcp"
      authorization-id="github-mcp-connection"
      context-variable-name="githubAuthorization"
      identity-type="managed"
      ignore-error="false" />

    <set-header name="Authorization" exists-action="override">
      <value>@("Bearer " + ((Authorization)context.Variables.GetValueOrDefault("githubAuthorization"))?.AccessToken)</value>
    </set-header>
    <set-header name="User-Agent" exists-action="override">
      <value>Azure-API-Management-MCP</value>
    </set-header>
    <set-header name="x-correlation-id" exists-action="override">
      <value>@((string)context.Variables["correlationId"])</value>
    </set-header>
  </inbound>

  <!-- The native APIM MCP server owns backend routing and Streamable HTTP. -->
  <backend>
    <base />
  </backend>

  <outbound>
    <base />

    <set-header name="x-correlation-id" exists-action="override">
      <value>@((string)context.Variables["correlationId"])</value>
    </set-header>

    <!--
      Convert upstream HTTP failures to JSON-RPC 2.0 errors. Do not include or
      inspect the upstream response body because doing so buffers MCP streams.
    -->
    <choose>
      <when condition='@(context.Response != null &amp;&amp; (int)context.Response.StatusCode >= 300)'>
        <set-header name="Content-Type" exists-action="override">
          <value>application/json</value>
        </set-header>
        <set-header name="Cache-Control" exists-action="override">
          <value>no-store</value>
        </set-header>
        <set-body>@{
          var status = (int)context.Response.StatusCode;
          var code = -32000;
          var message = "The upstream MCP server returned an HTTP error.";
          var category = "upstream";
          var retryable = false;

          if (status == 401 || status == 403)
          {
            code = -32001;
            message = "The upstream MCP server rejected the configured GitHub credential.";
            category = "authentication";
          }
          else if (status == 429)
          {
            code = -32002;
            message = "The upstream MCP server rate limit was exceeded.";
            category = "rate_limit";
            retryable = true;
          }
          else if (status >= 500)
          {
            code = -32003;
            message = "The upstream MCP server is temporarily unavailable.";
            category = "upstream";
            retryable = true;
          }

          JToken requestId;
          try
          {
            requestId = JToken.Parse((string)context.Variables["mcpRequestId"]);
          }
          catch
          {
            requestId = JValue.CreateNull();
          }

          return new JObject(
            new JProperty("jsonrpc", "2.0"),
            new JProperty("id", requestId),
            new JProperty("error", new JObject(
              new JProperty("code", code),
              new JProperty("message", message),
              new JProperty("data", new JObject(
                new JProperty("category", category),
                new JProperty("httpStatus", status),
                new JProperty("retryable", retryable),
                new JProperty("correlationId", (string)context.Variables["correlationId"])
              ))
            ))
          ).ToString(Newtonsoft.Json.Formatting.None);
        }</set-body>
        <set-status code="200" reason="OK" />
      </when>
    </choose>
  </outbound>

  <on-error>
    <set-header name="Content-Type" exists-action="override">
      <value>application/json</value>
    </set-header>
    <set-header name="Cache-Control" exists-action="override">
      <value>no-store</value>
    </set-header>
    <set-header name="x-correlation-id" exists-action="override">
      <value>@(context.Variables.GetValueOrDefault&lt;string&gt;("correlationId", context.RequestId.ToString()))</value>
    </set-header>
    <set-body>@{
      var correlationId = context.Variables.GetValueOrDefault&lt;string&gt;("correlationId", context.RequestId.ToString());
      var serializedRequestId = context.Variables.GetValueOrDefault&lt;string&gt;("mcpRequestId", "null");
      JToken requestId;

      try
      {
        requestId = JToken.Parse(serializedRequestId);
      }
      catch
      {
        requestId = JValue.CreateNull();
      }

      return new JObject(
        new JProperty("jsonrpc", "2.0"),
        new JProperty("id", requestId),
        new JProperty("error", new JObject(
          new JProperty("code", -32603),
          new JProperty("message", "API Management could not process the MCP request."),
          new JProperty("data", new JObject(
            new JProperty("category", "gateway"),
            new JProperty("retryable", false),
            new JProperty("correlationId", correlationId)
          ))
        ))
      ).ToString(Newtonsoft.Json.Formatting.None);
    }</set-body>
    <set-status code="200" reason="OK" />
  </on-error>
</policies>
